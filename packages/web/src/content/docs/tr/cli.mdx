---
title: CLI
description: nexode CLI seçenekleri ve komutları.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

nexode CLI herhangi bir argüman olmadan çalıştırıldığında varsayılan olarak [TUI](/docs/tui)'yi başlatır.

```bash
nexode
```

Ancak bu sayfada belgelendiği gibi komutları da kabul eder. Bu, nexode ile programlı olarak etkileşim kurmanıza olanak tanır.

```bash
nexode run "Explain how closures work in JavaScript"
```

---

### tui

nexode terminal kullanıcı arayüzünü başlatır.

```bash
nexode [project]
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                    |
| ------------ | ---- | --------------------------------------------------------------------------- |
| `--continue` | `-c` | Son oturuma devam et                                                        |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                 |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın) |
| `--prompt`   |      | Kullanılacak prompt                                                         |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                 |
| `--agent`    |      | Kullanılacak agent                                                          |
| `--port`     |      | Dinlenecek port                                                             |
| `--hostname` |      | Dinlenecek host adı                                                         |

---

## Komutlar

nexode CLI ayrıca aşağıdaki komutlara da sahiptir.

---

### agent

nexode için agent'ları yönetin.

```bash
nexode agent [command]
```

---

### attach

`serve` veya `web` komutlarıyla başlatılan, halihazırda çalışan bir nexode arka uç sunucusuna bir terminal ekleyin.

```bash
nexode attach [url]
```

Bu, TUI öğesinin uzak bir nexode arka ucuyla kullanılmasına olanak tanır. Örneğin:

```bash
# Start the backend server for web/mobile access
nexode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
nexode attach http://10.20.30.40:4096
```

#### Bayraklar

| Bayrak      | Kısa | Tanım                                |
| ----------- | ---- | ------------------------------------ |
| `--dir`     |      | TUI'yi başlatmak için çalışma dizini |
| `--session` | `-s` | Devam edecek oturum açma bilgileri   |

---

#### create

Özel yapılandırmaya sahip yeni bir agent oluşturun.

```bash
nexode agent create
```

Bu komut, özel sistem istemi ve araç yapılandırmasıyla yeni bir agent oluşturma konusunda size yol gösterecektir.

---

#### list

Mevcut tüm agent'ları listeleyin.

```bash
nexode agent list
```

---

### auth

Sağlayıcılar için kimlik bilgilerini ve oturum açmayı yönetme komutu.

```bash
nexode auth [command]
```

---

#### login

nexode, [Models.dev](https://models.dev) adresindeki sağlayıcı listesi tarafından desteklenmektedir, dolayısıyla kullanmak istediğiniz herhangi bir sağlayıcı için API anahtarlarını ayarlamak üzere `nexode auth login` kullanabilirsiniz. Bu `~/.local/share/nexode/auth.json`'de saklanıyor.

```bash
nexode auth login
```

nexode başlatıldığında sağlayıcıları kimlik bilgileri dosyasından yükler. Ayrıca ortam değişkenlerindeki veya projenizdeki `.env` dosyasındaki anahtarları da kullanır.

---

#### list

Kimlik bilgileri dosyasında depolanan tüm kimliği doğrulanmış sağlayıcıları listeler.

```bash
nexode auth list
```

Veya kısa versiyonu.

```bash
nexode auth ls
```

---

#### logout

Bir sağlayıcıyı kimlik bilgileri dosyasından temizleyerek oturumunuzu kapatır.

```bash
nexode auth logout
```

---

### github

Depo otomasyonu için GitHub aracısını yönetin.

```bash
nexode github [command]
```

---

#### install

GitHub aracısını deponuza yükleyin.

```bash
nexode github install
```

Bu komut gerekli GitHub Actions workflow'unu kurar ve yapılandırma adımlarında size rehberlik eder. [Daha fazla bilgi](/docs/github).

---

#### run

GitHub aracısını çalıştırın. Bu genellikle GitHub Eylemlerinde kullanılır.

```bash
nexode github run
```

##### Bayraklar

| Bayrak    | Açıklama                                    |
| --------- | ------------------------------------------- |
| `--event` | Aracıyı çalıştırmak için GitHub sahte olayı |
| `--token` | GitHub personal access token                |

---

### mcp

Model Bağlam Protokolü sunucularını yönetin.

```bash
nexode mcp [command]
```

---

#### add

Yapılandırmanıza bir MCP sunucusu ekleyin.

```bash
nexode mcp add
```

Bu komut, yerel veya uzak bir MCP sunucusu ekleme konusunda size yol gösterecektir.

---

#### list

Yapılandırılmış tüm MCP sunucularını ve bağlantı durumlarını listeleyin.

```bash
nexode mcp list
```

Veya kısa versiyonunu kullanın.

```bash
nexode mcp ls
```

---

#### auth

OAuth'un etkin olduğu bir MCP sunucusuyla kimlik doğrulaması yapın.

```bash
nexode mcp auth [name]
```

Sunucu adı belirtmezseniz mevcut OAuth özellikli sunucular arasından seçim yapmanız istenir.

Ayrıca OAuth özellikli sunucuları ve bunların kimlik doğrulama durumlarını da listeleyebilirsiniz.

```bash
nexode mcp auth list
```

Veya kısa versiyonunu kullanın.

```bash
nexode mcp auth ls
```

---

#### logout

MCP sunucusu için OAuth kimlik bilgilerini kaldırın.

```bash
nexode mcp logout [name]
```

---

#### debug

MCP sunucusu için OAuth bağlantı sorunlarının hatalarını ayıklayın.

```bash
nexode mcp debug <name>
```

---

### models

Yapılandırılmış sağlayıcıların tüm mevcut modellerini listeleyin.

```bash
nexode models [provider]
```

Bu komut, yapılandırdığınız sağlayıcılardaki modelleri `provider/model` formatında listeler.

Bu, [config dosyanızda](/docs/config/) kullanmanız gereken tam model adını bulmak için kullanışlıdır.

Modelleri bu sağlayıcıya göre filtrelemek için isteğe bağlı olarak bir sağlayıcı kimliğini iletebilirsiniz.

```bash
nexode models anthropic
```

#### Bayraklar

| Bayrak      | Tanım                                                                       |
| ----------- | --------------------------------------------------------------------------- |
| `--refresh` | Modeller.dev'den model önbelleğini yenileyin                                |
| `--verbose` | Daha ayrıntılı model çıktısı kullanın (maliyetler gibi meta veriler içerir) |

Önbelleğe alınan model listesini güncellemek için `--refresh` bayrağını kullanın. Bu, bir sağlayıcıya yeni modeller eklendiğinde ve bunları nexode'da görmek istediğinizde kullanışlıdır.

```bash
nexode models --refresh
```

---

### run

Doğrudan bir istem ileterek nexode'u etkileşimli olmayan modda çalıştırın.

```bash
nexode run [message..]
```

Bu, komut dosyası oluşturma, otomasyon veya TUI'un tamamını başlatmadan hızlı bir yanıt istediğinizde kullanışlıdır. Örneğin.

```bash "nexode run"
nexode run Explain the use of context in Go
```

Ayrıca, her çalıştırmada MCP sunucusunun soğuk önyükleme sürelerini önlemek için çalışan bir `nexode serve` örneğine de ekleyebilirsiniz:

```bash
# Start a headless server in one terminal
nexode serve

# In another terminal, run commands that attach to it
nexode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                          |
| ------------ | ---- | --------------------------------------------------------------------------------- |
| `--command`  |      | Çalıştırılacak komut, args için mesajı kullanın                                   |
| `--continue` | `-c` | Son oturuma devam et                                                              |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                       |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın)       |
| `--share`    |      | Oturumu paylaşın                                                                  |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                       |
| `--agent`    |      | Kullanılacak temsilci                                                             |
| `--file`     | `-f` | Mesaja eklenecek dosya(lar)                                                       |
| `--format`   |      | Biçim: varsayılan (biçimlendirilmiş) veya json (ham JSON olayları)                |
| `--title`    |      | Oturumun başlığı (değer sağlanmazsa kısaltılmış bilgi istemi kullanılır)          |
| `--attach`   |      | Çalışan bir nexode sunucusuna ekleyin (ör. http://localhost:4096)               |
| `--port`     |      | Yerel sunucunun bağlantı noktası (varsayılan olarak rastgele bağlantı noktasıdır) |

---

### serve

API erişimi için headless bir nexode sunucusu başlatır. Tam HTTP arayüzü için [server docs](/docs/server) sayfasına bakın.

```bash
nexode serve
```

Bu, TUI arayüzü olmadan nexode işlevselliğine API erişimi sağlayan bir HTTP sunucusunu başlatır. HTTP temel kimlik doğrulamasını etkinleştirmek için `NEXODE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `nexode` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### session

nexode oturumlarını yönetin.

```bash
nexode session [command]
```

---

#### list

Tüm nexode oturumlarını listeleyin.

```bash
nexode session list
```

##### Bayraklar

| Bayrak        | Kısa | Tanım                                  |
| ------------- | ---- | -------------------------------------- |
| `--max-count` | `-n` | En son N oturumla sınırla              |
| `--format`    |      | Çıkış formatı: tablo veya json (tablo) |

---

### stats

nexode oturumlarınız için belirteç kullanımı ve maliyet istatistiklerini gösterin.

```bash
nexode stats
```

#### Bayraklar

| Bayrak      | Açıklama                                                                                                    |
| ----------- | ----------------------------------------------------------------------------------------------------------- |
| `--days`    | Son N güne ait istatistikleri göster (tüm zamanlar)                                                         |
| `--tools`   | Gösterilecek araç sayısı (tümü)                                                                             |
| `--models`  | Model kullanım dökümünü göster (varsayılan olarak gizlidir). En üstteki N'yi göstermek için bir sayı iletin |
| `--project` | Projeye göre filtrele (tüm projeler, boş değer: mevcut proje)                                               |

---

### export

Oturum verilerini JSON olarak dışa aktarın.

```bash
nexode export [sessionID]
```

Bir oturum kimliği sağlamazsanız mevcut oturumlar arasından seçim yapmanız istenir.

---

### import

Bir JSON dosyasından veya nexode paylaşımından URL oturum verilerini içe aktarın.

```bash
nexode import <file>
```

Yerel bir dosyadan veya nexode paylaşımından (URL) içe aktarabilirsiniz.

```bash
nexode import session.json
nexode import https://opncd.ai/s/abc123
```

---

### web

Web arayüzüyle başsız bir nexode sunucusu başlatın.

```bash
nexode web
```

Bu, bir HTTP sunucusunu başlatır ve bir web arayüzü aracılığıyla nexode'a erişmek için bir web tarayıcısı açar. HTTP temel kimlik doğrulamasını etkinleştirmek için `NEXODE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `nexode` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### acp

Bir ACP (Ajan İstemci Protokolü) sunucusu başlatın.

```bash
nexode acp
```

Bu komut, nd-JSON kullanarak stdin/stdout aracılığıyla iletişim kuran bir ACP sunucusunu başlatır.

#### Bayraklar

| Bayrak       | Açıklama            |
| ------------ | ------------------- |
| `--cwd`      | Çalışma dizini      |
| `--port`     | Dinlenecek port     |
| `--hostname` | Dinlenecek host adı |

---

### uninstall

nexode'u kaldırın ve ilgili tüm dosyaları kaldırın.

```bash
nexode uninstall
```

#### Bayraklar

| Bayrak          | Kısa | Tanım                                           |
| --------------- | ---- | ----------------------------------------------- |
| `--keep-config` | `-c` | Yapılandırma dosyalarını sakla                  |
| `--keep-data`   | `-d` | Oturum verilerini ve anlık görüntüleri saklayın |
| `--dry-run`     |      | Nelerin kaldırılmadan kaldırılacağı göster      |
| `--force`       | `-f` | Onay istemlerini atla                           |

---

### upgrade

nexode'u en son sürüme veya belirli bir sürüme günceller.

```bash
nexode upgrade [target]
```

En son sürüme yükseltmek için.

```bash
nexode upgrade
```

Belirli bir sürüme yükseltmek için:

```bash
nexode upgrade v0.1.48
```

#### Bayraklar

| Bayrak     | Kısa | Açıklama                                               |
| ---------- | ---- | ------------------------------------------------------ |
| `--method` | `-m` | Kullanılan kurulum yöntemi: curl, npm, pnpm, bun, brew |

---

## Global bayraklar

nexode CLI aşağıdaki global bayrakları destekler.

| Bayrak         | Kısa | Tanım                                    |
| -------------- | ---- | ---------------------------------------- |
| `--help`       | `-h` | Yardımı görüntüle                        |
| `--version`    | `-v` | Sürüm numarasını yazdır                  |
| `--print-logs` |      | Günlükleri stderr'e yazdır               |
| `--log-level`  |      | Günlük düzeyi (DEBUG, INFO, WARN, ERROR) |

---

## Ortam değişkenleri

nexode ortam değişkenleri kullanılarak yapılandırılabilir.

| Değişken                              | Tip     | Açıklama                                                                    |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- |
| `NEXODE_AUTO_SHARE`                 | boolean | Oturumları otomatik olarak paylaş                                           |
| `NEXODE_GIT_BASH_PATH`              | string  | Windows'ta yürütülebilir Git Bash'in Yolu                                   |
| `NEXODE_CONFIG`                     | string  | Yapılandırma dosyasının yolu                                                |
| `NEXODE_TUI_CONFIG`                 | string  | TUI yapılandırma dosyasının yolu                                            |
| `NEXODE_CONFIG_DIR`                 | string  | Yapılandırma dizinine giden yol                                             |
| `NEXODE_CONFIG_CONTENT`             | string  | Satır içi JSON config içeriği                                               |
| `NEXODE_DISABLE_AUTOUPDATE`         | boolean | Otomatik güncelleme kontrollerini devre dışı bırak                          |
| `NEXODE_DISABLE_PRUNE`              | boolean | Eski verilerin temizlenmesini devre dışı bırak                              |
| `NEXODE_DISABLE_TERMINAL_TITLE`     | boolean | Otomatik terminal başlığı güncellemelerini devre dışı bırakın               |
| `NEXODE_PERMISSION`                 | string  | Satır içi JSON izin config'i                                                |
| `NEXODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Varsayılan eklentileri devre dışı bırakın                                   |
| `NEXODE_DISABLE_LSP_DOWNLOAD`       | boolean | Otomatik LSP sunucu indirmelerini devre dışı bırakın                        |
| `NEXODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Deneysel modelleri etkinleştir                                              |
| `NEXODE_DISABLE_AUTOCOMPACT`        | boolean | Otomatik context sıkıştırmayı devre dışı bırak                              |
| `NEXODE_DISABLE_CLAUDE_CODE`        | boolean | `.claude`'den okumayı devre dışı bırak (istem + beceriler)                  |
| `NEXODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | `~/.claude/CLAUDE.md` dosyasını okumayı devre dışı bırak                    |
| `NEXODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | `.claude/skills` yüklemesini devre dışı bırak                               |
| `NEXODE_DISABLE_MODELS_FETCH`       | boolean | Uzak kaynaklardan model getirmeyi devre dışı bırakın                        |
| `NEXODE_FAKE_VCS`                   | string  | Test amaçlı sahte VCS sağlayıcısı                                           |
| `NEXODE_DISABLE_FILETIME_CHECK`     | boolean | Optimizasyon için dosya süresi kontrolünü devre dışı bırakın                |
| `NEXODE_CLIENT`                     | string  | Client kimliği (varsayılan: `cli`)                                          |
| `NEXODE_ENABLE_EXA`                 | boolean | Exa web arama araçlarını etkinleştir                                        |
| `NEXODE_SERVER_PASSWORD`            | string  | `serve`/`web` için temel kimlik doğrulamayı etkinleştirin                   |
| `NEXODE_SERVER_USERNAME`            | string  | Temel kimlik doğrulama kullanıcı adını geçersiz kıl (varsayılan `nexode`) |
| `NEXODE_MODELS_URL`                 | string  | Model yapılandırmasını almak için özel URL                                  |

---

### Deneysel

Bu ortam değişkenleri değişebilecek veya kaldırılabilecek deneysel özellikleri etkinleştirir.

| Değişken                                        | Tip     | Tanım                                                   |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `NEXODE_EXPERIMENTAL`                         | boolean | Tüm deneysel özellikleri etkinleştir                    |
| `NEXODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Simge bulmayı etkinleştir                               |
| `NEXODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | TUI'da seçim yapıldığında kopyalamayı devre dışı bırak  |
| `NEXODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | MS cinsinden bash komutları için varsayılan zaman aşımı |
| `NEXODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | LLM yanıtları için maksimum çıktı belirteçleri          |
| `NEXODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Tüm dizin için dosya izleyiciyi etkinleştir             |
| `NEXODE_EXPERIMENTAL_OXFMT`                   | boolean | Oxfmt biçimlendiriciyi etkinleştir                      |
| `NEXODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Deneysel LSP aracını etkinleştir                        |
| `NEXODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Dosya izleyiciyi devre dışı bırak                       |
| `NEXODE_EXPERIMENTAL_EXA`                     | boolean | Deneysel Exa özelliklerini etkinleştirin                |
| `NEXODE_EXPERIMENTAL_LSP_TY`                  | boolean | Deneysel LSP tür denetimini etkinleştir                 |
| `NEXODE_EXPERIMENTAL_MARKDOWN`                | boolean | Deneysel işaretleme özelliklerini etkinleştir           |
| `NEXODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Plan modunu etkinleştir                                 |
