{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["CI", "NEXODE_DISABLE_SHARE"],
  "globalPassThroughEnv": ["CI", "NEXODE_DISABLE_SHARE"],
  "tasks": {
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "nexode#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@nexode-ai/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
